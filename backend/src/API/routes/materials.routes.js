import { Router } from "express";

const router = Router();

// router.get('/api/materials', undefined);
// router.post('/api/materials', undefined);
// router.patch('/api/materials/:id', undefined);
// router.delete('/api/materials/:id', undefined);

export default router;
