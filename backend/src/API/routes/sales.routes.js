import { Router } from "express";

const router = Router()

router.get('/api/sales', undefined)
router.post('/api/sales', undefined)

router.patch('/api/sales/:id', undefined)
router.delete('/api/sales/:id', undefined)

export default router